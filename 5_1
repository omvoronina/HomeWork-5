# 1. Пользователь вводит данные о количестве предприятий, их наименования и прибыль за 4 квартала (т.е. 4 отдельных числа)
# для каждого предприятия.. Программа должна определить среднюю прибыль (за год для всех предприятий) и вывести наименования предприятий,
# чья прибыль выше среднего и отдельно вывести наименования предприятий, чья прибыль ниже среднего.

n = int(input('Введите количество предприятий  '))
comp = {}
su = 0
for i in range(n):
    name = input('Введите наименование предприятия  ')
    income = [int(i) for i in (input('Введите через пробел прибыль предприятия за 1-й, 2-й, 3-й и 4й кварталы  ').split(' '))]
    comp[name] = [income, sum(income)]
    su += sum(income)
av = su / n
print(f"Средняя прибыль равна  {av} \n "
      f"Прибыль выше среднего у предприятий: {[j for j in comp.keys() if int(comp[j][1]) > av]}, \n "
      f"Прибыль ниже среднего у предприятий: {[j for j in comp.keys() if int(comp[j][1]) < av]}")
